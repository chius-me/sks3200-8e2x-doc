# 尚未确认

这些是 2.0.0.1 / A0 中文 WEBUI 核对之后，仍然不能写成确定行为的点。
适合作为后续 Issue 的清单。验证时请带上固件、是否提交、是否重启、是否抓包。

## 配置生效与持久化

- 「应用」「确认」「保存」各自的作用范围
- 未保存配置能活多久（刷新后？注销后？断电后？）
- 保存是否写入启动配置
- 状态页上的「保存」是否和配置页是同一个动作

## VLAN

- VLAN 1 不能选「非成员」的原因
- 改 PVID 之后，VLAN 1 是否允许移除成员
- VLAN 1 能否删除
- 设 Untagged 时是否自动改 PVID
- 同一端口能否成为多个 VLAN 的无标签成员
- 非成员口入站是否丢弃该 VID
- VID 0、4095 以及表单写 2–4094 但 VLAN 1 仍能编辑的规则
- 有没有隐藏的 ingress filtering
- 管理面是否固定走 VLAN 1；哪些 VLAN 的主机可以打开 Web（IPv4 页没有对应选项）

## 聚合

- 「静态 / LAG / LACP」的精确区分
- 两个都叫「端口类型」的字段里，那个 1–65535 数值到底是什么
- 聚合组 0 的含义
- LACP 短 / 长超时的实际秒数
- 散列算法
- 聚合口在 VLAN 页里如何表现

## 环路与 STP

- 环路检测与 STP 如何协作
- 「禁用全部」关的是哪一部分
- 时间间隔 / 恢复时间为 0 的含义
- 边缘端口是否跳过学习 / 侦听
- 状态列除「转发」外还会出现哪些词

## 风暴控制与 IGMP

- 表格里速率 0 表示未启用还是限速为 0
- 限速方向（入 / 出 / 双向）和计量粒度
- 编辑区默认「开启」和表格真实状态的关系
- 打开 IGMP 总开关之后会出现哪些新字段
- 「广播入组报文」转发到哪些口

## 维护操作

- 「恢复」是否保留管理 IP、用户、固件
- RST 孔短按 / 长按的时间与效果
- 导入配置、升级固件会不会重启，会不会保留 VLAN
- 下载配置 / 下载固件得到的文件格式
- 导出是否含密码

## 本次可见菜单之外

未见独立入口（不等于硬件或其它固件没有）：

ACL、QoS、802.1X、SNMP、Syslog、NTP / SNTP、IPv6、DHCP 服务器、三层路由、PoE、管理 VLAN、巨帧、HTTPS、多用户。

菜单 `nav` 空缺见 [菜单与页面路径](menu-map.md)。
