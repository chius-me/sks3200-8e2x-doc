# 尚未确认

2.0.0.1 / A0 中文 WEBUI 核对，再加上 [厂商手册 V2.0](vendor-manual.md) 之后，一部分内容从「完全不知道」变成了「厂商有定义、这里还没独立测」。下面这些仍然不能写成这台机器上的确定行为。

验证时请带上固件、是否提交、是否重启、是否抓包。

## 仍然开放

### 配置生效与持久化

- 未保存的「应用」能活多久（刷新？注销？断电？）
- 「保存」是否等于断电后仍在的启动配置（V2.0 只写 save the current configuration）
- 对话框里的「确认」是否等于页面上的「应用」

### VLAN

- VLAN 1 不能选「非成员」的原因
- 改 PVID 之后，VLAN 1 是否允许移除成员
- VLAN 1 能否删除
- 反向联动：设成无标签成员会不会改 PVID（V2.0 写的是 PVID → 无标签成员，没写反向）
- 同一端口能否成为多个 VLAN 的无标签成员
- 非成员口入站是否丢弃该 VID
- VID 0、4095 以及表单写 2–4094 但 VLAN 1 仍能编辑的规则
- 有没有隐藏的 ingress filtering
- 管理面是否固定走 VLAN 1；哪些 VLAN 的主机可以打开 Web（IPv4 页没有对应选项）

### 聚合

- 聚合组 **0** 的含义
- 散列算法
- LACP 主动 / 被动
- 聚合口在 VLAN 页里如何表现
- 本机真正建成过 LAG/LACP

### 环路与 STP

- 环路检测与 STP 如何协作（V2.0 只写按拓扑需要开启）
- 「禁用全部」关的是哪一部分
- 时间间隔 / 恢复时间为 0 的含义
- 边缘端口是否跳过学习 / 侦听
- 状态列除「转发」外还会出现哪些词

### 风暴控制与 IGMP

- 表格里速率 0 表示未启用还是限速为 0
- 限速方向（入 / 出 / 双向）和计量粒度
- 编辑区默认「开启」和表格真实状态的关系
- 打开 IGMP 之后的组表和额外字段（V2.0 截图有组表页，本次没打开）
- 「广播入组报文」具体洪泛到哪些口

### 维护操作

- 恢复出厂后管理 IP 是否回到 `192.168.10.12`、账号是否回到 `admin` / `admin`
- RST 孔短按 / 长按的时间与效果
- 导入配置会不会重启
- 下载配置 / 下载固件得到的文件格式
- 导出是否含密码
- 升级固件后 VLAN 是否还在（V2.0：写入成功后自动重启）

## 本次可见菜单之外

未见独立入口（不等于硬件或其它固件没有）：

ACL、QoS、802.1X、SNMP、Syslog、NTP / SNTP、IPv6 **配置页**（V2.0：系统信息可以显示 IPv6 地址）、DHCP 服务器、三层路由、本非 PoE 型号上的 PoE、管理 VLAN、巨帧、HTTPS、多用户。

菜单 `nav` 空缺见 [菜单与页面路径](menu-map.md)。
